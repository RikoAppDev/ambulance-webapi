kind: Service
apiVersion: v1
metadata:
  name: fd-ambulance-webapi
spec:
  selector:
    pod: fd-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port
